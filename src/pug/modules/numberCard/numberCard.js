class NumberCard {
    constructor(numberCard) {
        this.numberCard = numberCard;
        // this.slider = numberCard.querySelector('.number-card__slider-section');
    }
}


const numbersCards = document.querySelectorAll('.number-card');

// for (card in numbersCards) {

// }